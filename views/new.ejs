<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create New Post</title>
    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #121212 !important;
        color: #ffffff;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }

      .container {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-grow: 1;
        width: 100%;
        padding: 0;
        margin: 0;
      }

      .row {
        width: 100%;
        margin: 0 auto;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .col-8 {
        background-color: #1e1e1e;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
        padding: 20px 30px;
        border-radius: 8px;
        width: 66.666%;
        max-width: 600px; /* Ensure it doesn't get too wide on large screens */
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 15px;
      }

      input,
      textarea {
        background-color: #2e2e2e;
        border: 1px solid #444;
        border-radius: 4px;
        color: #ffffff;
        padding: 10px;
        font-size: 16px;
        outline: none;
        transition: border-color 0.3s;
      }

      input:focus,
      textarea:focus {
        border-color: #e84023;
      }

      .submitBtn {
        background-color: #e84023;
        color: #ffffff;
        border: none;
        padding: 10px;
        font-size: 16px;
        border-radius: 0.8rem;
        cursor: pointer;
        transition: background-color 0.3s;
      }

      .submitBtn:hover {
        background-color: #b82308;
      }

      .submitBtn:active {
        background-color: #aa1f07;
      }
    </style>
  </head>
  <body>
    <%- include("navbar.ejs") %>
    <div class="container">
      <div class="row mt-2 mb-2">
        <div class="col-8">
          <h2 style="color: #fff">Create New Post</h2>
          <form method="post" action="/posts">
            <input placeholder="Enter username" name="username" required />
            <input placeholder="Enter question" name="question" required />
            <textarea
              placeholder="Enter description"
              name="description"
              rows="4"
              required
            ></textarea>
            <button type="submit" class="submitBtn">Submit</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
